package androidx.activity;

import android.view.View;
import android.view.Window;

interface C {
    void a(Window window);

    void b(M m10, M m11, Window window, View view, boolean z10, boolean z11);
}
